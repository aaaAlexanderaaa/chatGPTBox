// Shape used by both the default config and the popup editor.
// Keep them in sync by importing from this single source of truth.

export const defaultExtractor = {
  name: '',
  urlPattern: '',
  method: 'auto',
  selectors: '',
  excludeSelectors: '',
  active: true,
}
